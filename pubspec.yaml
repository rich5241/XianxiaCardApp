name: xianxia_card_app
description: Xianxia Card Collection App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.2
  google_fonts: ^6.2.1
  csv: ^6.0.0 # 新增 CSV 解析套件

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/data/cards_data.csv
    - assets/Images/
    - assets/Videos/